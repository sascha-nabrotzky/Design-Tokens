{
    "scripts": {
        "build": "npm run tokens:validate-a11y && npm run tokens:flatten && npm run tokens:build:css",
        "tokens:validate-a11y": "node scripts/validate-a11y-tokens.js",
        "tokens:flatten": "node scripts/flatten-tokens.js",
        "tokens:build:css": "node scripts/build-css.mjs"
    },
    "devDependencies": {
        "@tokens-studio/sd-transforms": "^2.0.3",
        "style-dictionary": "^5.4.2"
    }
}
