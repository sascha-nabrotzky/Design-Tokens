{
    "scripts": {
        "build": "npm run tokens:flatten && style-dictionary build",
        "tokens:flatten": "node scripts/flatten-tokens.js"
    },
    "devDependencies": {
        "style-dictionary": "^5.4.2"
    }
}
